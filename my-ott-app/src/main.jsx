import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.jsx';  // Make sure this matches the updated file extension

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('app')
);
